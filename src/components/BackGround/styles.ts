import styled from "styled-components";

const Container = styled.div`
  display: flex;
  flex-direction: column;
  height: 100%;
`;

const CardContainer = styled.div`
  position: relative;
  width: 100%;
  height: 35vh;
`;

const BackgroundStyles = { Container, CardContainer };

export default BackgroundStyles;
